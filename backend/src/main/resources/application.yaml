spring:
  application:
    name: todo
  datasource:
    url: jdbc:postgresql://loc
    username: ${DB_USER}
    password: ${DB_PSW}
  liquibase:
    change-log: classpath:db/changelog/changelog.yaml
    enabled: true
    default-schema: public
  jpa:
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect:
              PostgreSQL10Dialect

logging.level.org.springframework.web: DEBUG
logging.level.org.hibernate: ERROR